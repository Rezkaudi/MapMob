/** How many categories each filter chip would show, before the chip itself is applied. */
export interface CategoryKindCounts {
  readonly all: number;
  readonly main: number;
  readonly sub: number;
}
